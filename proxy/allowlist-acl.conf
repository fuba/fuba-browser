# Allowlist mode ACL configuration for fuba-proxy
# This file is included by squid.conf when ALLOWLIST_MODE=true
# Only domains listed in allowlist.txt are permitted

acl allowed_domains dstdomain "/etc/fuba-proxy/allowlist.txt"
http_access deny !allowed_domains
